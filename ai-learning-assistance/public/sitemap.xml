import React from "react";
import { Download, SlidersHorizontal, ChevronLeft, ChevronRight } from "lucide-react";
import { Link } from "react-router-dom";
import { useAdmin } from "../context/AdminContext";

const clients = [
  {
    id: 1,
    name: "Alex Johnson",
    email: "alex.j@techflow.com",
    status: "ACTIVE",
    renewal: "Dec 15, 2024",
    daysLeft: 180,
    package: "Enterprise",
    avatar: "https://i.pravatar.cc/150?img=12",
  },
  {
    id: 2,
    name: "Maria Garcia",
    email: "m.garcia@studio.io",
    status: "EXPIRING",
    renewal: "Nov 28, 2024",
    daysLeft: 12,
    package: "Professional",
    avatar: "https://i.pravatar.cc/150?img=32",
  },
  {
    id: 3,
    name: "TechCorp Inc.",
    email: "billing@techcorp.com",
    status: "ACTIVE",
    renewal: "Jan 10, 2025",
    daysLeft: 206,
    package: "Enterprise",
    initials: "TC",
  },
  {
    id: 4,
    name: "John Smith",
    email: "jsmith.design@gmail.com",
    status: "EXPIRED",
    renewal: "Nov 15, 2024",
    daysLeft: 0,
    package: "Basic",
    avatar: "https://i.pravatar.cc/150?img=45",
  },
  {
    id: 5,
    name: "Sarah Williams",
    email: "sarah@williams.dev",
    status: "EXPIRING",
    renewal: "Nov 23, 2024",
    daysLeft: 7,
    package: "Professional",
    avatar: "https://i.pravatar.cc/150?img=22",
  },
];

const getStatusStyle = (status) => {
  switch (status) {
    case "ACTIVE":
      return "bg-green-100 text-green-700";
    case "EXPIRING":
      return "bg-yellow-100 text-yellow-700";
    case "EXPIRED":
      return "bg-red-100 text-red-600";
    default:
      return "bg-gray-100 text-gray-600";
  }
};

const getDaysStyle = (days) => {
  if (days === 0) return "text-red-600 font-semibold";
  if (days <= 15) return "text-orange-600 font-semibold";
  return "text-gray-800";
};

const ClientListing = () => {

     const { clients } = useAdmin();
  return (
    <div className="bg-gray-100  p-4 md:p-8">
      <div className="max-w-7xl mx-auto bg-white rounded-2xl shadow-sm border  border-gray-300 overflow-hidden">

        {/* HEADER */}
        <div className="flex flex-col md:flex-row md:items-center justify-between p-6 border-b border-gray-300 gap-4">
          <h2 className="text-xl font-semibold">Client Listings</h2>

          <div className="flex gap-3">
            <button className="flex items-center gap-2 px-4 py-2 border r border-gray-300ounded-lg hover:bg-gray-50 text-sm">
              <SlidersHorizontal size={16} /> Filter
            </button>
            <button className="flex items-center gap-2 px-4 py-2 border r border-gray-300ounded-lg hover:bg-gray-50 text-sm">
              <Download size={16} /> Export
            </button>
          </div>
        </div>

        {/* DESKTOP TABLE */}
        <div className="hidden md:block">
          <table className="w-full text-left">
            <thead className="bg-gray-50 text-gray-600 text-sm uppercase tracking-wider">
              <tr>
                <th className="p-2">Client Name</th>
                <th>Status</th>
                <th>Renewal Date</th>
                <th>Days Left</th>
                <th>Package</th>
              </tr>
            </thead>
            <tbody>
              {clients.map((client) => (
                <tr key={client.id} className="border-t border-gray-300 hover:bg-gray-50">
                  <td className="p-2">
                    <div className="flex items-center gap-4">
                      {client.avatar ? (
                        <img
                          src={client.avatar}
                          alt=""
                          className="w-12 h-12 rounded-xl object-cover"
                        />
                      ) : (
                        <div className="w-12 h-12 rounded-xl bg-indigo-100 flex items-center justify-center text-indigo-600 font-semibold">
                          {client.initials}
                        </div>
                      )}
                      <div>
                        <p className="font-medium">{client.name}</p>
                        <p className="text-sm text-gray-500">{client.email}</p>
                      </div>
                    </div>
                  </td>

                  <td>
                    <span className={`px-3 py-1 rounded-full text-xs font-medium ${getStatusStyle(client.status)}`}>
                      {client.status === "EXPIRING" ? "EXPIRING SOON" : client.status}
                    </span>
                  </td>

                  <td className="text-gray-700">{client.renewal}</td>

                  <td className={getDaysStyle(client.daysLeft)}>
                    {client.daysLeft} Days Left
                  </td>

                  <td className="text-gray-700">{client.package}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        {/* MOBILE CARDS */}
        <div className="md:hidden p-4 space-y-4">
          {clients.map((client) => (
            <Link to={'/client/details'} key={client.id} >
                <div className="border mb-4 border-gray-300 rounded-xl p-4 space-y-3">
              <div className="flex items-center gap-3">
                {client.avatar ? (
                  <img
                    src={client.avatar}
                    alt=""
                    className="w-12 h-12 rounded-lg"
                  />
                ) : (
                  <div className="w-12 h-12 rounded-lg bg-indigo-100 flex items-center justify-center text-indigo-600 font-semibold">
                    {client.initials}
                  </div>
                )}
                <div>
                  <p className="font-medium">{client.name}</p>
                  <p className="text-sm text-gray-500">{client.email}</p>
                </div>
              </div>

              <div className="flex justify-between text-sm">
                <span>Status</span>
                <span className={`px-2 py-1 rounded-full text-xs ${getStatusStyle(client.status)}`}>
                  {client.status === "EXPIRING" ? "EXPIRING SOON" : client.status}
                </span>
              </div>

              <div className="flex justify-between text-sm">
                <span>Renewal</span>
                <span>{client.renewal}</span>
              </div>

              <div className="flex justify-between text-sm">
                <span>Days Left</span>
                <span className={getDaysStyle(client.daysLeft)}>
                  {client.daysLeft} Days
                </span>
              </div>

              <div className="flex justify-between text-sm">
                <span>Package</span>
                <span>{client.package}</span>
              </div>
              </div>
            </Link>
          ))}
        </div>

        {/* FOOTER / PAGINATION */}
        <div className="flex flex-col md:flex-row items-center justify-between p-6 border-t border-gray-300 gap-4 text-sm text-gray-600">
          <p>Showing 1-5 of 1,284 clients</p>

          <div className="flex items-center gap-2">
            <button className="p-2 border r border-gray-300ounded-md hover:bg-gray-50">
              <ChevronLeft size={16} />
            </button>

            <button className="px-3 py-2 bg-gray-900 text-white rounded-md">1</button>
            <button className="px-3 py-2 border r border-gray-300ounded-md hover:bg-gray-50">2</button>
            <button className="px-3 py-2 border r border-gray-300ounded-md hover:bg-gray-50">3</button>

            <button className="p-2 border r border-gray-300ounded-md hover:bg-gray-50">
              <ChevronRight size={16} />
            </button>
          </div>
        </div>

      </div>
    </div>
  );
};

export default ClientListing;